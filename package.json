{
  "name": "flash-mask",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
